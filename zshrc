alias gs='git status'
alias gl='git l'

# only add a key to the agent for 12 hours
alias ssh-add-12='ssh-add -t 43200 ~/.ssh/id_ed25519'

# have prompt display the current directory name
export PROMPT="%1~: "
